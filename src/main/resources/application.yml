#DB config
spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: C##SPOONS
    password: SPOONS

#file upload config
servlet:
  multipart:
    max-file-size: 100000000MB
    max-request-size: 1000000MB
    location: C:/subproject99

# Mybatis config
mybatis:
  mapper-locations: mappers/**/*.xml

# image upload dir
image:
  image-dir: C:/subproject99/src/main/resources/static/